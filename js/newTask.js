


export default function newTask(addBtn) {
    
    document.addEventListener("click", e => {

         if(e.target.matches(addBtn)){

            let color = document.querySelector(".color-input").value;
            let text = document.querySelector(".name-input").value;

        }
    
    });
}